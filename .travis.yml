language: generic
sudo: required 
services:
  - docker

before_install:
  - docker build -t javokhir226/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true javokhir226/docker-react npm run test
